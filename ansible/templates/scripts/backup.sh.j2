#!/bin/sh

systemctl stop minecraft

tar cf - --exclude=bluemap --exclude=cache -S server | gzip --best -c | oci os object put --auth instance_principal --bucket-name {{ bucket_name }} --file - --name "minecraft.tar.gz" --force

systemctl start minecraft
