#!/bin/sh

dnf check-update
if [ "$?" = "100" ] ; then
	systemctl stop minecraft-{{ edition }}
	dnf -y update

	if [ ! "$(dnf needs-restarting -r)" ] ; then
		reboot
	else
		systemctl start minecraft-{{ edition }}
	fi
else
	exit
fi
