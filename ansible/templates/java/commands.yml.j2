# This is the commands configuration file for Bukkit.
# For documentation on how to make use of this file, check out the Bukkit Wiki at
# https://www.spigotmc.org/go/commands-yml
#
# If you need help on this file, feel free to join us on irc or leave a message
# on the forums asking for advice.
#
# IRC: #spigot @ irc.spi.gt
#    (If this means nothing to you, just go to https://www.spigotmc.org/go/irc )
# Forums: https://www.spigotmc.org/
# Bug tracker: https://www.spigotmc.org/go/bugs

{% if global_overrides.commands is defined and overrides.commands is defined %}{{ commands | combine(global_overrides.commands, overrides.commands, recursive=True) | to_nice_yaml(indent=2) }}{% elif global_overrides.commands is defined %}{{ commands | combine(global_overrides.commands, recursive=True) | to_nice_yaml(indent=2) }}{% elif overrides.commands is defined %}{{ commands | combine(overrides.commands, recursive=True) | to_nice_yaml(indent=2) }}{% else %}{{ commands | to_nice_yaml(indent=2) }}{% endif %}