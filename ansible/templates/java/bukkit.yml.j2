# This is the main configuration file for Bukkit.
# As you can see, there's actually not that much to configure without any plugins.
# For a reference for any variable inside this file, check out the Bukkit Wiki at
# https://www.spigotmc.org/go/bukkit-yml
#
# If you need help on this file, feel free to join us on irc or leave a message
# on the forums asking for advice.
#
# IRC: #spigot @ irc.spi.gt
#    (If this means nothing to you, just go to https://www.spigotmc.org/go/irc )
# Forums: https://www.spigotmc.org/
# Bug tracker: https://www.spigotmc.org/go/bugs


{% set combined_bukkit = bukkit %}
{% if global_overrides.bukkit is defined %}
{% set combined_bukkit = (combined_bukkit | combine(global_overrides.bukkit, recursive=True)) %}
{% endif %}
{% if node_overrides.bukkit is defined %}
{% set combined_bukkit = (combined_bukkit | combine(node_overrides.bukkit, recursive=True)) %}
{% endif %}
{% if outer_item.bukkit is defined %}
{% set combined_bukkit = (combined_bukkit | combine(outer_item.bukkit, recursive=True)) %}
{% endif %}

{{ combined_bukkit | to_nice_yaml(indent=2) }}